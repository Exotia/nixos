# Editor used by CLI
export SUDO_EDITOR="$EDITOR"
export BAT_THEME=ansi

# Duplicated from .config/uwsm/env so SSH works too
export OMARCHY_PATH=$HOME/.local/share/arch
export PATH=$OMARCHY_PATH/bin:$PATH:$HOME/.local/bin
