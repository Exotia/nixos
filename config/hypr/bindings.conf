# Application bindings
bind = SUPER, RETURN, uwsm-app -- xdg-terminal-exec --dir="$(nix-cmd-terminal-cwd)"
bind = SUPER ALT, RETURN, uwsm-app -- xdg-terminal-exec --dir="$(nix-cmd-terminal-cwd)" bash -c "tmux attach || tmux new -s Work"
bind = SUPER SHIFT, RETURN, nix-launch-browser
bind = SUPER SHIFT, F, uwsm-app -- nautilus --new-window
bind = SUPER ALT SHIFT, F, uwsm-app -- nautilus --new-window "$(nix-cmd-terminal-cwd)"
bind = SUPER SHIFT, B, nix-launch-browser
bind = SUPER SHIFT ALT, B, nix-launch-browser --private
bind = SUPER SHIFT, M, nix-launch-or-focus spotify
bind = SUPER SHIFT, N, nix-launch-editor
bind = SUPER SHIFT, D, nix-launch-tui lazydocker
bind = SUPER SHIFT, G, nix-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bind = SUPER SHIFT, O, nix-launch-or-focus ^obsidian$ "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bind = SUPER SHIFT, W, uwsm-app -- typora --enable-wayland-ime
bind = SUPER SHIFT, SLASH, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bind = SUPER SHIFT, A, launch-webapp "https://chatgpt.com"
bind = SUPER SHIFT ALT, A, launch-webapp "https://grok.com"
bind = SUPER SHIFT, C, launch-webapp "https://app.hey.com/calendar/weeks/"
bind = SUPER SHIFT, E, launch-webapp "https://app.hey.com"
bind = SUPER SHIFT, Y, launch-webapp "https://youtube.com/"
bind = SUPER SHIFT ALT, G, nix-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bind = SUPER SHIFT CTRL, G, nix-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bind = SUPER SHIFT, P, nix-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bind = SUPER SHIFT, X, launch-webapp "https://x.com/"
bind = SUPER SHIFT ALT, X, launch-webapp "https://x.com/compose/post"

# Add extra bindings
# bind = SUPER SHIFT, R, alacritty -e ssh your-server

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bind = SUPER, SPACE, exec, nix-menu
